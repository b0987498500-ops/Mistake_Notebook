# 規則與異動日誌 (Rules Log)

此檔案用來記錄 Miley 錯題本的所有整理規則變更，以便日後追溯。

## 📅 2026-07-26 首次建立
*   **【規則 01】基本運作流程**：
    *   使用者發送題目圖片時，AI 應先進行**詳細解題**。
    *   解題完成後，將錯題整理至對應的文字檔中，標註：年級、科目、單元、核心知識點。
*   **【規則 02】錯題本記錄格式（更新於 2026-07-26）**：
    *   建立學科分類錯題本（如：`math.md` 記錄數學錯題）。
    *   每道錯題的排版順序必須嚴格遵循：
        1. 【題目敘述】（包含題目文字、選項與幾何題圖檔）
        2. 【🏷️ 錯題分類】（年級、科目、單元、核心知識點）
        3. 【🔑 答案】
        4. 【💡 完整解析】（含思考盲點剖析）
        5. 【原始截圖連結】
*   **【規則 03】LaTeX 與樣式優化（更新於 2026-07-26）**：
    *   錯題本 .md 檔案中全面啟用標準 LaTeX 語法（例如使用 $ 或 $$ 標註數學公式）以呈現精美數學公式與科學記號。
    *   考量預覽器相容性，不再使用 GitHub-style 警告語法（如 `[!WARNING]`），全面改為相容性最高之「通用 Blockquote（以 `>` 開頭）+ Emoji 圖示 + 粗體標題」作為顏色與區塊標記。
*   **【規則 04】對話排版順序調整**：
    *   在對話回答中，AI 必須將「分類歸檔資訊」置於最前方的區塊。
    *   分類歸檔資訊呈現完畢後，再開始進行詳細的解題步驟。
*   **【規則 05】新增思考盲點剖析區塊**：
    *   錯題本中若有合適的觀念陷阱或使用者提出的延伸思考，應新增「🧠 思考盲點與概念釐清」區塊。
*   **【規則 06】AI 對話回覆禁用 LaTeX 數學符號**：
    *   為防止非預覽模式下的對話視窗渲染失敗，AI 在與使用者進行**對話回覆時，依然嚴格禁止使用 $ 符號**，所有變數與公式一律在對話中以純文字表示。
*   **【規則 07】題目圖片拆分與備份規則（更新於 2026-07-26）**：
    *   使用者上傳的原始圖片會作為原始記錄，命名為 `科目_題號_source.png`（如 `math_3_source.png`）。
    *   AI 應對原始圖片進行裁剪（只保留純題目與幾何圖形，裁掉答案與解析），命名為 `科目_題號.png`（如 `math_3.png`）嵌入至錯題本的題目敘述中，方便 Miley 自行做題複習。
    *   在該題目的最尾端，必須新增一個相對路徑的一鍵點擊超連結按鈕指向原始圖檔：`[🖼️ 查看原始完整截圖](images/科目_題號_source.png)`，方便在預覽器中一鍵開啟。
    *   若該題不需要幾何圖形，則只保留原始圖檔 `_source.png` 作為追溯存檔，不在錯題本中嵌入圖片，以節省版面空間。

*   **【規則 08】動態題號與圖檔碼寬規則（新增於 2026-07-26）**：
    *   錯題編號與檔案名稱（包括圖片名稱）採用「動態自適應位數」。
    *   目前從第 1 題開始，統一採用 1 碼（即 1, 2, 3...，不補零）。
    *   當題目累積到達第 9 題、第 99 題等臨界值後，AI 將啟動「擴展測試」，準備升級為 2 碼（10-99）、3 碼（100-999）或未來的 4 碼（1000-9999），藉此驗證 AI 的動態重構與程式能力。

*   **【規則 09】快取與預覽問題排除（新增於 2026-07-26）**：
    *   若日後遇到 Markdown 預覽、圖片或檔案更新不即時等快取問題，提醒使用者使用以下解法：
        1. 在 VS Code 中按下快捷鍵 `Ctrl + Shift + P`（開啟命令面板）。
        2. 輸入並選擇 `Developer: Reload Window`（開發人員: 重新載入視窗）以清除快取並重載工作區。

*   **【規則 10】重複錯題處理與二刷/三刷標記（新增於 2026-07-27）**：
    *   **主動偵測**：當使用者新增或整理題目時，AI 必須主動掃描現有學科錯題本，比對核心內容與圖片。若發現重複，則不重複新增區塊。
    *   **二刷/三刷累計**：將原題目標題加上或更新為 `[二刷]`、`[三刷]`，並在原題目區塊的 `📅 記錄日期` 下方記錄新的日期（如 `- **二刷日期**：YYYY-MM-DD`）。
    *   **手動/複習重錯觸發**：當使用者在複習時告知某題又錯或解不出來時，AI 亦需主動找到該題並執行此二刷/三刷累加邏輯。

*   **【規則 11】附圖原地放大對照與雙視窗檢視規則（更新於 2026-08-05）**：
    *   **純 CSS 互動式原地放大/縮小**：錯題本中所有嵌入圖片（包含題目圖、解答圖、示意圖）不再採用會觸發超連結跳頁的 `<a>` 標籤包裹，改為純 CSS Checkbox Hack（藉由隱藏的 Checkbox 控制 `img` 寬度）。圖片預設以縮圖尺寸（`max-width: 350px`/`450px`）顯示；使用者**點擊圖片本身**即可就地平滑放大至 100% 滿版，再次點擊圖片即可縮回。點擊 `<summary>` 可展開/收合整個圖片區塊。
    *   **側邊雙視窗獨立對照**：提示與引導使用者，在 VS Code 預覽中點擊文字超連結開啟圖片分頁後，**按住 Tab 分頁往右側拖曳**或按 **`Ctrl + \`** 分割編輯器，以進行完美雙視窗對照。






